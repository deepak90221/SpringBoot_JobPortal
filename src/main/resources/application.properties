# Application Name
spring.application.name=SpringJobportal

# ===== Database =====
spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/jobportal}
spring.datasource.username=${DB_USER:root}
spring.datasource.password=${DB_PASSWORD:Kingdeepak!@!17}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# ===== File Upload =====
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# ===== Mail Configuration =====
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USER:ch.jyothi2015@gmail.com}
spring.mail.password=${MAIL_PASS:hyjv sprl zmwv qnpq}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# ===== Server Port =====
server.port=${PORT:8888}  # Dynamic port for Render, fallback 8888 locally
